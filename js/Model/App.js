'use strict'

class App {
    constructor () {
        this.groups = [];
        this.setting = [];
    }
    
    addGroupe () {
        // groups.push(new Group());
    }

    addSetting () {
        // groups.push(new Setting());
    }
}