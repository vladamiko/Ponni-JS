'use strict';

class App {
    constructor () {
        this.groups = [];
        this.setting = [];
    }
    
    addGroupe () {
        // groups.push(new Group());
        console.log('ADED');
    }

    addSetting () {
        // groups.push(new Setting());
    }
}
module.exports = App;